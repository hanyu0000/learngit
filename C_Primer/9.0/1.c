#include<stdio.h>
/*void order(double *a,double *b,double *c){
    double temp;
    if(*a>*b){
        temp = *a;
        *a = *b;
        *b = temp;
    }
    if(*a>*c){
        temp = *a;
        *a = *c;
        *c = temp;
    }
    if(*b>*c){
        temp = *b;
        *b = *c;
        *c = temp;
    }
}
int main(){
    double a,b,c;
    printf("输入3个浮点数:");
    scanf("%lf%lf%lf",&a,&b,&c);
    order(&a,&b,&c);
    printf("a=%f b=%f c=%f\n",a,b,c);
    return 0;                                                                                                 
}*/
int main(){
    
}